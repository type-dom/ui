export const $tdRadioButtonActive = {
  // backgroundColor: radioGroup?.fill || '',
  // borderColor: radioGroup?.fill || '',
  // boxShadow: radioGroup?.fill ? `-1px 0 0 0 ${radioGroup.fill}` : '',
  // color: radioGroup?.textColor || '',
};
